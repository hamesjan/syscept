This was used with docker ubuntu 

pull the ubuntu image from the docker hub.

Run the setup script to install everything

```
$ bash setup.sh

```

To Exit docker

From within container
```

$ exit

```

To restart 

```

$ docker start -ai syscept_container

```